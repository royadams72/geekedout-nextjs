name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "19"

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test

      - name: Build project
        run: npm run build

    # - name: Deploy
    #   if: github.ref == 'refs/heads/main'
    #   env:
    #     YOUR_API_KEY: ${{ secrets.YOUR_API_KEY }}
    #   run: |
    #     # Run deployment script or use an action for your deployment platform
    #     npm run deploy
